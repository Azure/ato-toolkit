# Required metadata
sonar.projectKey=~project-c12-runtime
sonar.projectName=Simple project config for SonarQube Runner
sonar.projectVersion=1.0

# Comma-separated paths to directories with sources (required)
sonar.sources=src

# Language
sonar.language=go

# Encoding of the source files
sonar.sourceEncoding=UTF-8
